<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--Movie Dao-->
    <bean id="getAllMoviesSql" class="java.lang.String">
        <constructor-arg value="SELECT m.id, m.name_russian, m.name_native, m.year, m.rating, m.price, p.picture_path
                                FROM movieland.movie m
                                LEFT JOIN movieland.poster p ON m.id = p.movie_id"/>
    </bean>

    <bean id="getThreeRandomMoviesSql" class="java.lang.String">
        <constructor-arg value="SELECT m.id, m.name_russian, m.name_native, m.year, m.rating, m.price, p.picture_path
                                FROM movieland.movie m
                                LEFT JOIN movieland.poster p ON m.id = p.movie_id
                                ORDER BY RANDOM() LIMIT 3;"/>
    </bean>

    <bean id="getMoviesByGenreIdSql" class="java.lang.String">
        <constructor-arg value="SELECT m.id, m.name_russian, m.name_native, m.year, m.rating, m.price, p.picture_path
                                FROM movieland.movie m
                                LEFT JOIN movieland.poster p ON m.id = p.movie_id
                                JOIN  movieland.movie_genre mg ON m.id = mg.movie_id
                                WHERE mg.genre_id = :genre_id"/>
    </bean>


    <bean id="getMovieByIdSql" class="java.lang.String">
        <constructor-arg value="SELECT m.id, m.name_russian, m.name_native, m.year, m.rating, m.price, p.picture_path, m.description
                                FROM movieland.movie m
                                LEFT JOIN movieland.poster p ON m.id = p.movie_id
                                WHERE m.id = :id;"/>
    </bean>

    <bean id="movieInsertSql" class="java.lang.String">
        <constructor-arg value="INSERT INTO movieland.movie (name_russian, name_native, year, description, rating, price)
                                VALUES (:name_russian, :name_native, :year, :description, :rating, :price);"/>
    </bean>

    <bean id="getNewestMovieIdSql" class="java.lang.String">
        <constructor-arg value="SELECT max(m.id) as id FROM movieland.movie m;"/>
    </bean>

</beans>